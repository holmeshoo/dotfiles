# --- Global SSH Configuration ---

Host *
    # 接続を維持する設定
    ServerAliveInterval 60
    ServerAliveCountMax 3
    # 自動でパスワード入力を省略するための鍵の共有（必要に応じて）
    # ControlMaster auto
    # ControlPath ~/.ssh/master-%r@%h:%p

# --- Includes ---
# 秘密のホスト設定などは別ファイルに切り出せるようにする
Include config.local

# --- Examples ---
# Host myserver
#     HostName 1.2.3.4
#     User myname
#     IdentityFile ~/.ssh/id_rsa
